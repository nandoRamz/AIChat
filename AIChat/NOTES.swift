//
//  NOTES.swift
//  AIChat
//
//  Created by Nando on 1/28/25.
//

//Creating a luanch screen: Create a launch screen from templates. In the info plist look for a launch screen property. There are 2 Launch Screen and Launch Screen interface file base name (use this one if using a story board.

//Creating reusable views and returning values: I didn't know you can return values from a generic view. For example var content: (some value) -> T. Usually used it like this () -> T didn't really know () was a closure that could return values
